/*
there might be necessary modules that the app depends on 
(bootcamp 4 registered a 'listings' dependency here, but we don't have anything really like that)
*/

/* register the application and inject all the necessary dependencies */
var app = angular.module('TwitterData', []);